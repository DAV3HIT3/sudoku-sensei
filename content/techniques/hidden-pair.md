# Hidden Pair

When two digits can each go in only the same two cells of a unit, those two cells must hold those two digits. Every other candidate can be removed from them.

Hidden pairs are harder to see than naked ones, because the extra candidates hide them. Look for two digits that have the same two possible cells in a unit. Once the extras are removed, the hidden pair becomes a naked pair.
