# 2-String Kite

Take a digit with exactly two possible cells in a row and exactly two in a column, where one end of the row pair and one end of the column pair are in the same box. Those two ends see each other, so at most one holds the digit, which means at least one of the two far ends does.

Any cell that sees both far ends cannot hold the digit.
