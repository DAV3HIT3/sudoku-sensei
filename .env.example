# The dev database on home-db, over the LAN. The password is printed once by
# add-app.sh; ask whoever ran it, or rotate it (see home-db/add-app.sh).
DATABASE_URL=postgres://sudokusensei:<password>@192.168.50.99:5432/sudokusensei
# Stands in for the Tailscale identity header in development.
DEV_USER_LOGIN=you@github
