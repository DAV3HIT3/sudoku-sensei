# The dev database on home-db, over the LAN. Never the production one. The
# password is printed once by add-app.sh; rotate it there if it is lost.
DATABASE_URL=postgres://sudokusensei_dev:<password>@192.168.50.99:5432/sudokusensei_dev
# Stands in for the Tailscale identity header in development.
DEV_USER_LOGIN=you@github
